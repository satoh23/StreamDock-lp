# 公開先の URL。canonical / OGP / sitemap の絶対 URL の元になる。
# ⚠ Cloudflare Pages にデプロイして実際のサブドメインが決まったら、必ずここを直すこと。
#   ここが実際の URL と違うと、X などのシェアカードが画像を取得できない（相対パスでも同じ）。
VITE_SITE_URL=https://streamdock.pages.dev
